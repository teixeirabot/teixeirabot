services:
  - type: web
    name: agentmesh
    runtime: python
    rootDir: agent_social_mvp
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
